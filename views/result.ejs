<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Calculation Result</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>

  <div class="resultDiv">
    <h2>Calculation Result</h2>
    <p><strong>Subscribers:</strong> <%= amount %></p>
    <p><strong>Currency:</strong> <%= currency %></p>

    <% if (usedVolumePricing) { %>
      <p><strong>Price per User (Annual):</strong> <%= (multipliedAmount / amount).toFixed(4) %> <%= currency %></p>
      <p><strong>Total Annual Price:</strong> <%= multipliedAmount.toFixed(2) %> <%= currency %></p>
      <p><em>(Volume subscription prices are annual and include discounts)</em></p>
    <% } else { %>
      <p><strong>Price per User (Monthly):</strong> <%= price.toFixed(4) %> <%= currency %></p>
      <p><strong>Discount:</strong> <%= multi %>%</p>
      <p>Base price: <%= price.toFixed(4) %> x <%= amount %> = <%= (price * amount).toFixed(2) %> <%= currency %></p>
      <p><strong>Monthly Price (after discount):</strong> <%= multipliedAmount.toFixed(2) %> <%= currency %></p>
      <p><strong>Annual Price (monthly x 12):</strong> <%= (multipliedAmount * 12).toFixed(2) %> <%= currency %></p>
    <% } %>
  </div>

  <form action="/" method="GET">
    <button class="secondary-button" type="submit">Return</button>
  </form>

</body>
</html>
